import logging
logging.getLogger('MplBlit').addHandler(logging.NullHandler())

# WARN = logging.WARN
# INFO = logging.INFO
# DEBUG = logging.DEBUG
# ERROR = logging.ERROR
# CRITICAL = logging.CRITICAL

# Filter = logging.Filter

# def set_level(level):
#     """equivalent to setting numerical level. see logging library"""
#     l = logging.getLogger('fastAGC')
#     l.setLevel(level)
#     return l